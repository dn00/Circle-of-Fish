using UnityEngine;
using System.Collections;

public class heartScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	//GetComponent<UISprite>().fillAmount = 0;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
